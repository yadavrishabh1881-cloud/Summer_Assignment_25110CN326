#include <stdio.h>

int main() {
    int a ,b ;
    printf("Enter the number whoose tabel wants to print :");
    scanf("%d", &a );
    for ( int i = 1 ; i <=10 ; i++)
     { b = a * i ;
     printf("%dX%d = %d \n" , a , i , b );
     }
    return 0;
}
