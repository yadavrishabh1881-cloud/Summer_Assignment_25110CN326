#include<stdio.h>
int main() {
int a , sum = 0 ;
printf("Enter the Natural Number:");
scanf("%d", &a );
for( int i = 1 ; i<=a ; i++)
{ sum = i + sum ; }
printf("The of a Natural number is : %d : ", sum );
return 0 ;
}
